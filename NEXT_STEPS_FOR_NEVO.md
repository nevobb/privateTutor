# Next Steps For Nevo

## What Nevo Should Review Next
1. **RTL Layout Implementation:** Open the app locally and verify the right-to-left layout structure aligns with the "Academic Minimalist" design expectations. Ensure the Material Panel is correctly positioned on the right and the Memory Panel on the left.
2. **Type Segregation Architecture:** Review `/src/types/index.ts` to confirm that `AcademicKnowledgeItem` and `LearnerMemory` are structurally isolated to your satisfaction, preventing cross-contamination.
3. **Behavior Skeleton Tests:** Inspect `/tests/behavior.test.ts`. This file outlines the behavioral regressions we want to prevent. Please confirm if these test cases represent your core product priorities.
4. **Mock Interaction Flow:** Interact with the chat in different `WorkModes` (e.g., Practice vs. Research) to see the mock responses and citation formatting.

## What Decisions Still Require Nevo
1. **Schema Deepening:** How complex does the `LearnerMemoryObservation` schema need to be? Should it include numeric confidence scoring or specific tagging dimensions?
2. **Citation Mapping:** How exactly will citations map back to source documents when we implement the real RAG solution? Will we use page numbers, bounding boxes, or semantic text chunks?
3. **Cost Mode Execution:** How does `CostMode` map functionally to the backend? Does it determine which specific Gemini model is invoked, or just the token limits and search depth?
4. **Design System Tweaks:** Final approval of the color palette and typography (Libre Caslon Text & Work Sans) generated by Stitch.

## What Should NOT Be Implemented Yet
- **Do not** connect the actual Gemini API.
- **Do not** connect Firebase Auth, Firestore, or Storage.
- **Do not** implement Genkit or LangChain logic.
- **Do not** write real PDF parsing/OCR code.
- **Do not** build real persistent storage logic.
- **Do not** add authentication or user management UI.
- **Do not** expand the project into a complex dashboard or LMS-style UI. Focus remains strictly on the core tutor view.
