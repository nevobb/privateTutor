# Branch Workflow

- `main` is the stable base branch.
- Jules must not work directly on `main`.
- Every new task must use a new branch.
- PR base should be `main`.
- PR compare/head should be the task branch.
- The current feature branch was used as the initial bootstrap state.
